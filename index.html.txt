
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Для Амана</title>
    <style>
        body {
            margin: 0; font-family: -apple-system, sans-serif; height: 100vh;
            display: flex; justify-content: center; align-items: center;
            background: linear-gradient(135deg, #fde2e4, #fbc4d4); overflow: hidden;
        }
        .page {
            display: none; text-align: center; padding: 30px;
            background: rgba(255,255,255,0.95); border-radius: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1); max-width: 400px; width: 85%;
            position: relative; z-index: 2; animation: fadeIn 0.6s ease-out;
        }
        .active { display: block; }
        .photo { width: 150px; height: 150px; border-radius: 20px; object-fit: cover; border: 4px solid #ff5d8f; margin-bottom: 15px; }
        h2 { color: #ff4b6e; margin-bottom: 10px; }
        .letter-text { text-align: left; font-size: 16px; line-height: 1.6; color: #444; }
        button {
            margin-top: 20px; padding: 15px 40px; border: none; border-radius: 50px;
            background: #ff5d8f; color: white; cursor: pointer; font-size: 18px;
            font-weight: bold; -webkit-tap-highlight-color: transparent;
        }
        canvas { position: fixed; top: 0; left: 0; pointer-events: none; z-index: 1; }
        @keyframes fadeIn { from { opacity: 0; transform: scale(0.9); } to { opacity: 1; transform: scale(1); } }
    </style>
</head>
<body>

<canvas id="confetti"></canvas>

<div class="page active" id="page1">
    <h2>Для Амана ❤️</h2>
    <p>Небольшой сюрприз для тебя</p>
    <button id="openBtn">Открыть</button>
</div>

<div class="page" id="page2">
    <img class="photo" src="https://i.ibb.co/VWK5rtNQ/IMG-3574.jpg" alt="Photo">
    <h2>Письмо</h2>
    <div class="letter-text">
        Дорогой Аман,<br><br>
        Хочу, чтобы ты знал — каждый день с тобой делает меня счастливой.
        Хотя мы и условились не отмечать этот праздник, я всё равно хотела тебя немного порадовать.<br><br>
        Очень тебя люблю и сильно скучаю. Чмок тебя в Родриго. 😉<br><br>
        С любовью, <b>Эля 💖</b>
    </div>
</div>

<script>
window.onload = function() {
    const btn = document.getElementById("openBtn");
    const page1 = document.getElementById("page1");
    const page2 = document.getElementById("page2");
    const canvas = document.getElementById("confetti");
    const ctx = canvas.getContext("2d");

    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;

    let hearts = [];

    function drawHeart(x, y, size, color) {
        ctx.fillStyle = color;
        ctx.beginPath();
        ctx.moveTo(x, y + size / 4);
        ctx.quadraticCurveTo(x, y, x + size / 4, y);
        ctx.quadraticCurveTo(x + size / 2, y, x + size / 2, y + size / 4);
        ctx.quadraticCurveTo(x + (size * 3) / 4, y, x + size, y + size / 4);
        ctx.quadraticCurveTo(x + size, y + size / 2, x + size / 2, y + size);
        ctx.quadraticCurveTo(x, y + size / 2, x, y + size / 4);
        ctx.fill();
    }

    btn.addEventListener("click", function() {
        page1.classList.remove("active");
        page2.classList.add("active");
        for (let i = 0; i < 40; i++) {
            setTimeout(addHeart, i * 100);
        }
    });

    function addHeart() {
        hearts.push({
            x: Math.random() * canvas.width,
            y: canvas.height + 20,
            size: Math.random() * 15 + 10,
            speed: Math.random() * 2 + 1,
            color: `hsl(${Math.random() * 20 + 340}, 80%, 65%)`,
            drift: Math.random() * 1 - 0.5
        });
    }

    function update() {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        hearts.forEach((h, index) => {
            h.y -= h.speed;
            h.x += h.drift;
            drawHeart(h.x, h.y, h.size, h.color);
            if (h.y < -20) hearts.splice(index, 1);
        });
        requestAnimationFrame(update);
    }
    update();
};
</script>
</body>
</html>
